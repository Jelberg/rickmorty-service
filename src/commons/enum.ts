export enum TYPES {
  EPISODES = 'episodes',
  CHARACTERS = 'characters',
}

export enum STATUS {
  ACTIVE = 'active',
  SUSPENDED = 'suspended',
  CANCELLED = 'cancelled',
}

export enum CATEGORIES {
  SPECIE = 'specie',
  SEASON = 'season',
}
